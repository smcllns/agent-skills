# Previous name: md-asks

`md-asks` was the previous technical name for **Markdown Agent Tags** (`atag`).

Use [atag.md](atag.md) for the current naming rules. New technical surfaces should use `atag`; user-facing prose should say `@agent` tag or just "tag."
