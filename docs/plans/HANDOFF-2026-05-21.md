# Handoff — markdown-agent-directives plugin, 2026-05-21

You're picking this up mid-pivot. Sam has just materially rewritten `SKILL.md` and is switching to Opus to continue.

## TL;DR

We're shipping the `markdown-agent-directives` skill as a Claude plugin (its first plugin from the `smcllns/skills` marketplace). The bare skill is on `markdown-agent-comments-plugin` branch, ready for review. **Plugin form has not been scaffolded yet** — that's the main remaining work.

Tests pass: 27/27 (`bun test skills/markdown-agent-directives/reference/directives-spec.test.ts`).

## Current branch

`markdown-agent-comments-plugin` (the branch name is from before the rename — left alone for continuity, merges to main either way).

Recent commits (newest first):
- `9314292` Sync test/spec to Sam's SKILL.md rewrite (3-agent list, restructure)
- `9d8346d` Sam's manual edits to SKILL.md (3-agent trigger list, new sections, backwards-compat rule)
- `4e9b298` Reorder SKILL.md to lead with what+how, push scan mechanics later
- `6296794` Rename markdown-agent-comments → markdown-agent-directives + narrow scope
- `cc6d2ee` (now superseded) Switch scan grep → awk for context-aware shorthand

## Where things stand

### Skill content (Sam's authoring as of `9d8346d`)

`skills/markdown-agent-directives/SKILL.md` — the bare skill. **Sam authored this directly**; don't restructure it without asking. Key things in his current shape:

1. **3-agent trigger list** — only `#agent`, `#claude`, `#codex` trigger the scan. `pi` and `hermes` are still mentioned as familiar reply names (`@pi`, `@hermes`) but are not directive triggers.
2. **New section: "Directive Shapes" table** — 3-row summary: `#agent` / `[!NOTE]` / `[!DONE]`.
3. **New section: "Unresolved rule"** — explicit definition.
4. **New section: "Resolution Contract"** — what to do per unresolved match.
5. **New section: "If further input required"** — handles "take a turn" and "can't act" cases.
6. **New section: "Best practices"** — collects don't-self-reply, don't-cap-results, work-goes-in-body, familiar-name rules, AND a new **backwards-compatibility** rule:

   > "The scan will pick up `[!NOTE]` callouts in docs. If there is no sign of a `#agent` directive in the title however, it will be considered a standard markdown callout, not an active thread requiring action."

   This means **the agent must check for a `#agent` mention before treating any `[!NOTE]` callout as actionable.** Pure markdown callouts (note-taking, asides) are picked up by the scan but skipped by the agent.

### Test infrastructure

`reference/directives-spec.md` — human-readable spec with `md @test:match` / `md @test:nomatch` fenced-block fixtures.

`reference/directives-spec.test.ts` — bun:test. Hardcodes `SCAN_REGEX` and `AGENTS`; consistency-checks SKILL.md contains both verbatim.

`reference/wishlist.md` — parked "speaker labels" (HTML cite tags) idea, unchanged.

Run with: `bun test skills/markdown-agent-directives/reference/directives-spec.test.ts`

### Plan doc

`docs/plans/2026-05-19-markdown-agent-directives-plugin.md` — the original phase-by-phase plan. Phases 1-5 (scaffold, marketplace, verify, scheduled, ship) are the remaining work.

## Open items Sam may want addressed

In rough order from highest-value to nice-to-have:

1. **(Possible inconsistency Sam may want to fix himself)** In SKILL.md the example callout (lines 22-29) has a directive line with a leading double-space (`>  #claude ...`) and trailing whitespace on some quoted blank lines. Compare with the cleaner version at lines 97-99 (no extra spaces). Could be intentional but might be drift. **Don't fix without asking** — Sam is authoring this section directly.
2. **(Possible inconsistency)** SKILL.md "Unresolved rule" says: *"A shorthand inline #agent directive that has not yet been wrapped in a `[!DONE]-` callout."* The word "shorthand" is leftover from the previous scope; can probably drop. Ask Sam.
3. **(Behavior gap)** The backwards-compat rule (Best practices, last item) says agent must check for `#agent` mention in `[!NOTE]` title before actioning. This is a NEW protocol step. Right now the test fixtures don't cover this — every `[!NOTE]` fixture has an `#agent` reference, so they'd all be actionable per the new rule. Worth adding a fixture for "plain `[!NOTE]` without #agent — picked up by scan but agent skips it (rule, not regex)."
4. **(Stale text)** `directives-spec.md` "Word starting with an agent prefix" fixture was updated from `#piling` → `#agency`. The prose is consistent but the explanation could be tightened.

## What's remaining (the actual plugin work)

Phase 1 onward from `docs/plans/2026-05-19-markdown-agent-directives-plugin.md`. Summary:

- **Phase 1 — Scaffold the plugin.** Create `claude-cowork-plugins/markdown-agent-directives/` with `.claude-plugin/plugin.json`, copy `SKILL.md` + `reference/` in, write plugin README. No hooks (stateless).
- **Phase 2 — Wire into marketplace.** Add entry to `.claude-plugin/marketplace.json` (currently `"plugins": []`). Update root README Plugins section.
- **Phase 3 — Verify locally.** Install in Cowork via local-folder. Test with a real markdown file that has a `#claude` directive.
- **Phase 4 — Verify scheduled execution.** This is the WHY of the plugin — Cowork scheduled task invokes the skill on a vault path.
- **Phase 5 — Ship.** PR to main, sync back to `~/Projects/dotfiles/skills/` (dotfiles still has the OLD name `markdown-agent-comments` with OLD scope; full overwrite at merge time).

## Process notes

- **Commit at each step.** Sam expects atomic commits with clear messages.
- **Don't push directly to main.** PRs only.
- **Tests must pass before commits.** Run `bun test skills/markdown-agent-directives/reference/directives-spec.test.ts` after any change that touches SKILL.md, the regex, or the spec.
- **The dotfiles version at `~/Projects/dotfiles/skills/markdown-agent-comments/` is still on the OLD scope.** Don't sync mid-flight. Sync at merge time.
- **Don't auto-restructure SKILL.md.** Sam is now authoring it directly. Treat his edits as canonical.
- **Sister branches:**
  - `inbox-zero` (parked, plugin work for inbox-zero, comes after this ships)
  - `plugin-marketplace-scaffold` already merged via PR #7

## Quick context for new agent

Read these in order:
1. This handoff doc
2. `docs/plans/2026-05-19-markdown-agent-directives-plugin.md` — phase plan
3. `skills/markdown-agent-directives/SKILL.md` — the skill itself (Sam's authoring)
4. `docs/architecture.md` — repo layout, skill-vs-plugin distinction
5. `docs/writing-claude-plugins.md` — accumulated plugin-authoring know-how
6. `claude-cowork-plugins/` is currently empty on this branch (the `inbox-zero` branch has the inbox-zero plugin as precedent; can be inspected for plugin structure but isn't on main)
